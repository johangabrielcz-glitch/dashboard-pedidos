/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'standalone' // asegura que Vercel construya un build completo de Next.js
}

module.exports = nextConfig;
